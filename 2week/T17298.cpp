#include <bits/stdc++.h>
using namespace std;

int n;
int a;
vector<pair<int, int>> v[1000004];

int main(){

    cin >> n;
    for(int i=0; i<n; i++) {
        cin >> a;
        v[i].push_back({a, -1});
    
    }

    for(int i=0; i<n; i++){
        int b = v[i];

        
    }

    return 0;
}