#include <bits/stdc++.h>
using namespace std;

int n, c; // 메시지의 길이n과 최대값c
int x;
int main() {
    cin >> n >> c;
    map<int, int> a;
    for(int i=0; i<n; i++){
        cin >> x;
        
        // a.insert(x, );
    }
}