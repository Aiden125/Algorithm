#include <bits/stdc++.h>
using namespace std;

int m, n, a;
int main(){
    cin >> n >> m;
    vector<int> v[n];
    
    
    for(int i=0; i<n; i++){
        cin >> a;
        v[i].push_back(a);
    }

    while(n+1){
        int b = v[n];
        

        n--;
    }

    

    

    return 0;
}